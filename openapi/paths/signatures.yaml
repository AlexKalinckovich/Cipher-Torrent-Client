sign:
  post:
    operationId: signTorrent
    summary: Подписать торрент своим ключом
    parameters:
      - name: infoHash
        in: path
        required: true
        schema:
          type: string
    responses:
      '201':
        description: Подпись создана
        content:
          application/json:
            schema:
              $ref: '../openapi.yaml#/components/schemas/SignatureCreateResponse'
      '404':
        description: Торрент не найден
        content:
          application/json:
            schema:
              $ref: '../openapi.yaml#/components/schemas/ApiError'

collection:
  get:
    operationId: getTorrentSignatures
    summary: Список подписей для торрента
    parameters:
      - name: infoHash
        in: path
        required: true
        schema:
          type: string
    responses:
      '200':
        description: Массив подписей
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../openapi.yaml#/components/schemas/TorrentSignature'